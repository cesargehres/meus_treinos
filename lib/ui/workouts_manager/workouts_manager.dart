import 'package:flutter/material.dart';

class WorkoutsManager extends StatelessWidget {
  const WorkoutsManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
